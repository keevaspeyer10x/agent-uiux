# Agent UI/UX MCP Server configuration
# Non-secret environment variables.
# Secrets (Google Drive OAuth, API keys) come from agent.env via Infisical.

UIUX_ALLOWED_EMAILS=keeva.speyer@10xlabs.com.au
# Set to "true" only for local dev without OAuth. Fail-closed by default.
# UIUX_ALLOW_UNAUTHENTICATED=true
